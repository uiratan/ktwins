project_name: ktwins

builds:
  - id: ktwins
    main: ./cmd/ktwins
    binary: ktwins
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]

archives:
  - id: default
    format: tar.gz
    files:
      - README.md
      - CHANGELOG.md
      - LICENSE*

checksum:
  name_template: "checksums.txt"

changelog:
  use: changelog
  sort: asc
  abbrev: 0

release:
  # owner/name are auto-detected from the git remote; override if needed.
  prerelease: false
  name_template: "Version {{ .Tag }}"
